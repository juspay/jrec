name: "CI"
on:
  pull_request:
  push:
jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix: 
        ghc: ['default', 'ghc8101']
        os: [ubuntu-latest]
    name: GHC $${{ matrix.ghc }}
    steps:
    - uses: actions/checkout@v2
    - uses: cachix/install-nix-action@v10
    - name: Build 🔧
      run: nix-build -j4
